# Heterogeneous Model

初始化一组模型族放在列表中。每个客户端按顺序领走一个模型：

```python
# models.py -> class BaseHeadSplit
self.base = eval(args.models[cid % len(args.models)])
```

取模型的方式也比较巧妙：

`args.models[index]`是一个字符串，字符串里放的是模型定义文本。通过`eval()`把字符串的引号去掉，就变成了一个对象。



关于feature dim的处理：

首先将最后一个fc层变为AdaptiveAvgPool1d，调整输出维度为`args.feature_dim`

而后重新初始化分类器

```python
        if hasattr(self.base, 'heads'):
            head = self.base.heads
            self.base.heads = nn.AdaptiveAvgPool1d(args.feature_dim)
        elif hasattr(self.base, 'head'):
            head = self.base.head
            self.base.head = nn.AdaptiveAvgPool1d(args.feature_dim)
        elif hasattr(self.base, 'fc'):
            head = self.base.fc
            self.base.fc = nn.AdaptiveAvgPool1d(args.feature_dim)
        elif hasattr(self.base, 'classifier'):
            head = self.base.classifier
            self.base.classifier = nn.AdaptiveAvgPool1d(args.feature_dim)
        else:
            raise('The base model does not have a classification head.')

        if hasattr(args, 'heads'):
            self.head = eval(args.heads[cid % len(args.heads)])
        elif 'vit' in args.models[cid % len(args.models)]:
            self.head = nn.Sequential(
                nn.Linear(args.feature_dim, 768), 
                nn.Tanh(),
                nn.Linear(768, args.num_classes)
            )
        else:
            self.head = nn.Linear(args.feature_dim, args.num_classes)
```

从上述代码中可以看到，只要不是在args中手动指定的heads以及vit，其他的都会使用homogeneous classifier。其中args.heads在main.py的选择模型异质性的场景中选择。

在`main.py`中的模型异构设置中，`HtFE`指的是只有encoder是异构的，classifier会初始化为相同的。

# 文件结构

`--save_folder_name=temp`用于保留训练过程中的临时量，比如客户端模型，prototype等文件。在服务器端通过这些文件来load客户端模型。在训练结束后，会删除这些文件

`args.dataset`是所有训练过程中创建文件/文件夹的标识符。加载数据集也用它标识。后续改为数据集+noniid的组合。

`results/{args.dataset}_{args.algorithm}_test_{repeat_num}.h5`：用于保存一次训练的结果。

# 需要添加内容

## 1. Logger & tensorboard

当前训练过程中只有print。只有完全训练结束后才会保留一个训练过程。需要加入一个logger和tensorboard可视化训练过程。

## 2. seed

当前没有设置seed选项。

## 3. 训练过程中以及结束后的结果保存文件夹

1.`args.dataset`中加入场景，如`Cifar10_dir_0.1`, `Cifar10_pat_2`

2.保存训练结果文件夹(`serverbase.py -> save_results`) `results/{args.dataset}_{args.algorithm}_test_{repeat_num}.h5`：将`repeat_num`改为`seed`

3.训练过程临时文件夹(`server -> args.save_folder_name_full`) `temp/{args.dataset}/{args.algorithm}/{time.time()}`：将`seed`加入进去。